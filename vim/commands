# Vim Commands

Mode Sensitive:

default/command mode = esc
Write mode = insert (I)

search = /serach_term
case insensitive search = /term\c

go to line number = :line_number

In Command Mode:
copy a line  = y + y
cut a line = d + d
paste a line  = p 
save = :w
quit = :q
quit without changes = :q!
save & quit = :wq

